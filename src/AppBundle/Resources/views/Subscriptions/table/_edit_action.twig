<button class="btn btn-xs btn-secondary ml-1" data-toggle="modal" data-target="#edit-subscription-modal" data-id="{{ id }}"
        data-subscription-service-id="{{ subscriptionServiceId }}"
        data-url="{{ path('subscription_api_patch', {'id': id, 'subscription_service_id': subscriptionServiceId}) }}"
        data-redirect="{{ dataRedirect }}">
  <svg class="icon icon-sm icon-300">
    <use xlink:href="/bootstrap-italia/dist/svg/sprite.svg#it-pencil"></use>
  </svg>
  <span>{{ 'modifica' | trans }}</span>
</button>
