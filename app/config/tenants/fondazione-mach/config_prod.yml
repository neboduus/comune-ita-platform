imports:
  - { resource: ../../parameters.yml }
  - { resource: ../../common/security.yml }
  - { resource: ../../common/monolog.yml }
  - { resource: ../../services.yml }
  - { resource: ../../bundles/ }
  - { resource: "@AppBundle/Resources/config/services.yml" }
  - { resource: ../../config.yml }


parameters:
  prefix: fondazione-mach
  codice_meccanografico: fem
  database_name: sdc_fem

framework:
  router:
    resource: "%kernel.root_dir%/config/common/routing.yml"
    strict_requirements: true

twig:
  globals:
    endpoint_prefix: '%prefix%'

services:
  ocsdc.protocollo.handler: "@ocsdc.protocollo.handlers.pitre"
