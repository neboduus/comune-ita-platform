{% if app.user.roles is defined %}
    <div class="header-slim-right-zone">
        <div class="it-access-top-wrapper">
            {% if 'ROLE_CPS_USER' in app.user.roles %}
                <div class="dropdown">
                    <a class="btn btn-dropdown dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icon icon-lg">
                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-expand') }}"></use>
                        </svg>
                        <span>{{ app.user.fullName }}</span>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <div class="link-list-wrapper">
                            <ul class="link-list">
                                <li>
                                    <a class="list-item left-icon" href="{{ path('user_profile') }}">
                                        <svg class="icon icon-sm icon-primary left">
                                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-user') }}"></use>
                                        </svg>
                                        <span>Profilo</span>
                                    </a>
                                </li>
                                <li>
                                    <a class="list-item left-icon" href="{{ path('user_logout') }}">
                                        <svg class="icon icon-sm icon-primary left">
                                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-link') }}"></use>
                                        </svg>
                                        <span>Logout</span>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            {% elseif ('ROLE_OPERATORE' in app.user.roles) or ('ROLE_ADMIN' in app.user.roles) %}
                <div class="dropdown">
                    <a class="btn btn-dropdown dropdown-toggle" href="#" role="button" id="dropdownMenuLink"
                       data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icon icon-lg">
                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-expand') }}"></use>
                        </svg>
                        <span class="d-none d-lg-block">{{ app.user.fullName }}</span>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                        <div class="link-list-wrapper">
                            <ul class="link-list">
                                <li>
                                    <a class="list-item left-icon" href="{% if ('ROLE_ADMIN' in app.user.roles) %}{{ path('admin_user_profile_show') }}{% else %}{{ path('operatori_user_profile_show') }}{% endif %}">
                                        <svg class="icon icon-sm icon-primary left">
                                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-user') }}"></use>
                                        </svg>
                                        <span>Profilo</span>
                                    </a>
                                </li>
                                <li>
                                    <a class="list-item left-icon" href="{{ logout_path('operatori') }}">
                                        <svg class="icon icon-sm icon-primary left">
                                            <use xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-link') }}"></use>
                                        </svg>
                                        <span>Logout</span>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
{% else %}
    <div class="header-slim-right-zone">
        <div class="it-access-top-wrapper">
            <a data-login-top-button="" class="btn btn-icon btn-full" href="{{ path('user_dashboard') }}"
               title="Esegui il login al sito" style="">
                                 <span class="rounded-icon">
                                     <svg class="icon icon-primary"><use
                                                 xlink:href="{{ asset('bootstrap-italia/dist/svg/sprite.svg#it-user') }}"></use></svg>
                                </span>
                <span class="d-none d-lg-block">Accedi all'area personale</span>
            </a>
        </div>
    </div>
{% endif %}
