<style>
  .modal .form-group {
    margin-bottom: 0;
    padding-bottom: 0;
  }
</style>

<div class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog" aria-labelledby="myLargeModalLabel"
     aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="p-4">
        <div class="row">
          <div class="col-lg-12 px-lg-4 py-lg-2">
            <h1 id="modalTitle"></h1>
            <p>{{ 'messages.mail_to_user' | trans}} <a
                href="mailto:{{ pratica.user.emailContatto }}">{{ pratica.user.emailContatto }}</a></p>
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12 px-lg-4 py-lg-2">
            {{ form_start(outcomeForm) }}
            <div class="d-none">{{ form_row(outcomeForm.outcome) }}</div>
            {{ form_row(outcomeForm.message) }}
            <small
              class="form-text text-muted mb-3">{{ 'operatori.flow.approva_o_rigetta.motivazione.esito_help' | trans }}</small>
            <div class="form-group">
              <label>{{ 'operatori.flow.approva_o_rigetta.motivazione.allega_label' | trans }}</label>
              {{ form_row(outcomeForm.attachments) }}
            </div>
            <div id="vueApp" class="px-2">
              {% if outcomeForm.vars.helper.vueApp %}
              <{{ outcomeForm.vars.helper.vueApp }} id="upload_outcome_attachment">
            </{{ outcomeForm.vars.helper.vueApp }}>
            {% endif %}
            <small class="form-text text-muted mb-3">{{ 'operatori.flow.approva_o_rigetta.motivazione.allega_help' | trans }}</small>
            {% if outcomeForm.payment_amount is defined  %}
              {{ form_row(outcomeForm.payment_amount) }}
              <small class="form-text text-muted mb-3">{{ 'operatori.flow.approva_o_rigetta.motivazione.payment_amount_help' | trans }}</small>
            {% endif %}
          </div>
          <div class="row justify-content-between mt-3">
            <div class="col-auto align-self-center">
              <strong>
                <svg class="icon icon-sm">
                  <use xlink:href="/bootstrap-italia/dist/svg/sprite.svg#it-warning-circle"></use>
                </svg>
                {{ 'messages.warning_pratice' | trans }}
              </strong>
            </div>
            <div class="col-auto">
              <button type="submit" class="btn btn-primary float-right">{{ 'messaggi.invia' | trans }}</button>
            </div>
          </div>
          {{ form_rest(outcomeForm) }}

          {{ form_end(outcomeForm) }}
        </div>
      </div>
    </div>
  </div>
</div>
</div>

<div class="modal fade" id="integration-request-modal" tabindex="-1" role="dialog"
     aria-labelledby="IntegrationRequestModal" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="p-4">
        <div class="row">
          <div class="col-lg-12 px-lg-4 py-lg-2">
            <h1>{{ 'operatori.inegrations_request' | trans }}</h1>
            <p>{{ 'messages.mail_to_user' | trans}} <a
                href="mailto:{{ pratica.user.emailContatto }}">{{ pratica.user.emailContatto }}</a></p>
          </div>
        </div>
        <div class="row">
          <div class="col-lg-12 px-lg-4 py-lg-2">
            {{ form_start(integration_request_form) }}
            {{ form_row(integration_request_form.message) }}
            <small class="form-text text-muted mb-3">
             {{ 'operatori.send_request_integration_description' | trans | raw }}
            </small>
            <div class="row justify-content-between mt-3">
              <div class="col-auto">
                <button type="submit" class="btn btn-primary float-right">{{ 'operatori.send_request_integration' | trans }}</button>
              </div>
            </div>
            {{ form_rest(integration_request_form) }}

            {{ form_end(integration_request_form) }}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
